    <div class="form-group">
        <label for="{@label_for}}">{@label_value}}</label>
        <input type="{@input_type}}" name="{@input_name}}" id="{@input_id}}" value="{@input_value}}" placeholder="{@input_placeholder}}" maxlength="{@input_maxlength}}" class="form-control" readonly/>
    </div>